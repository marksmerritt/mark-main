<div class="page-header">
  <h1>Edit Note</h1>
</div>

<div class="card">
  <%= render "form", note: @note, notebooks: @notebooks, tags: @tags,
             form_url: note_path(@note["id"]), form_method: :patch,
             submit_label: "Update Note", cancel_path: note_path(@note["id"]) %>
</div>
