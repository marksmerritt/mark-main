<div class="page-header">
  <h1>
    <span class="material-icons-outlined" style="font-size: 1.5rem; vertical-align: -4px; color: var(--primary);">add</span>
    New Template
  </h1>
</div>

<%= form_with url: note_templates_path, method: :post do |f| %>
  <%= render "form", f: f %>
<% end %>
